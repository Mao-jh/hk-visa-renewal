---
name: hk-visa-renewal
description: 香港身份续签·风险预审（按次收费，2.99 元/次）。当用户问「高才通续签要什么条件」「我不在香港住能续签吗」「优才续签会被拒吗」「IANG 过期还能续吗」「两址两单是什么」「挂靠续签有什么风险」时使用。输入签证类型（高才通A/B/C/优才/IANG/专才）与个人情况（在港天数、工作与收入、雇佣形态、受养人、首签时间），返回续签风险报告：风险等级与置信度、逐条风险点（入境处执行口径条目编号+相似案例编号+补强建议）、六类签证续签要求对照表、续签必知（专属问卷/学历认证/两址两单）、断签后果说明。
runtime: remote http
endpoint: https://airpriv.top/api/hk-visa-renewal
cost: 2.99 CNY / per_call
---

# 香港身份续签·风险预审（集成支付版）

本 Skill 是支付宝 AI 收「自研商家」架构的**薄壳**：本地只做编排与转发，
支付与履约全部由后端完成，**不持有私钥、不下单、不碰资金**——
支付全程走支付宝官方 `alipay-bot` CLI 与官方收银台，信任面干净。

**本 Skill 已内嵌完整支付能力**：无需用户另行安装任何官方支付 Skill。

## 工作模型

先确定当前状态，再按状态行动：

| 当前状态 | 下一步 |
|---|---|
| 用户问续签条件/断签风险/两址两单（未发起请求） | 按「调用前必采字段」收集画像，然后按「调用流程」第 1 步发起请求 |
| 收到 HTTP 402 / Payment-Needed | 读取 `references/402-payment.md` 并执行其中的支付流程 |
| 已有待确认支付，用户继续操作或询问结果 | 读 `references/402-payment.md`，从「查询支付状态」继续 |
| 其他 Skill 已在处理支付 | 不要重复执行支付命令 |

## 调用前必采字段（触发付费前必须完成）

本工具的输入是**个人画像**而非文本——画像残缺会直接毁掉报告质量。发起请求前，
从对话中确认以下要素；用户没说全就先追问（一次问清，不要挤牙膏）：

| 要素 | 必采 | 追问示例 |
|---|---|---|
| 签证类型（必须明确到细分） | 是 | 「你的高才通是 A 类、B 类还是 C 类？」（A=年薪250万+；B=百强学士有工作经验；C=百强学士应届毕业；说不清就逐项帮用户对号） |
| 在港情况 | 是 | 「平时住在香港还是内地？大概一年在港多少天？在港有租房吗？」 |
| 工作与收入 | 是 | 「在香港是受雇、自雇还是没工作？大概月收入多少？有报税和强积金吗？」 |
| 受养人 | 否 | 「配偶孩子是受养人身份在港吗？」 |
| 首签获批时间 | 否 | 「首签是什么时候批的？」（用于判断到期窗口） |

把收集到的要素组织成 `situation`（≥50 字自然段）+ 结构化参数。用户明确不愿说的字段留空，
**不要替用户编造或脑补**。

## 调用流程（对 Agent）

1. 向后端 `POST https://airpriv.top/api/hk-visa-renewal`，body：

```json
{
  "situation": "<个人画像自然段：在港天数/住址/工作状态/月收入/受养人>",
  "visa_type": "<高才A/高才B/高才C/优才/IANG/专才>",
  "first_approval_date": "<可选：首签获批年月，如 2024-06>",
  "request_id": "<本次唯一ID，如 UUID>"
}
```

2. 返回 `402` → 读取 `references/402-payment.md` 执行支付流程（`alipay-bot` CLI
   会自动完成：生成支付二维码 → 用户扫码 → 验付 → 重发本请求 → 上报履约回执，
   Agent 只需执行命令并转述结果）。
3. 支付成功后 CLI 自动返回结构化风险报告（无需手动带 proof 重发）。

**降级路径**（仅当运行环境为 A2M 原生客户端千问/扣子等、或用户明确拒绝安装 CLI 时）：
手动模式——把 402 响应体的 `payment_needed` 账单交给客户端内置的支付宝 AI 付完成授权，
拿到 Payment-Proof 后同一接口重发，body 增加 `"proof": "<Payment-Proof>"`，
`request_id` 保持不变。

## 参数

| 参数 | 必填 | 说明 |
|---|---|---|
| situation | 是（≥50 字） | 在港天数/住址/工作状态（受雇/自雇/创业/无业）/月收入/受养人 |
| visa_type | 是 | 高才A/高才B/高才C/优才/IANG/专才（决定适用口径集；「高才通」未注明细分也可，报告会提示差异） |
| first_approval_date | 否 | 首签获批年月（YYYY-MM，用于判断到期窗口） |
| request_id | 是 | 幂等键，同一画像的两次请求保持一致 |
| proof | 仅降级模式 | 支付宝支付凭证 |

## 输出结构（交付约定 · Agent 转述规则）

- `risk_level`：低 / 中 / 高（附 `confidence`：high/mid/low）。
- `hits[]`：每条风险点含 `用户原话事实 → 入境处口径条目编号（PV-xx）→ 相似案例编号（VR-xx）→ 续签影响 → 补强建议`。
- `requirements_table`：六类签证续签要求对照表（含当前适用行）。
- `must_know`：续签必知清单（高才专属问卷、B/C 类学历认证、两址两单四项缺一即驳回等）。
- `rejection_consequence`：断签后果（高才断签终身无法再申请、受养人连坐等）。
- `disclaimer`：免责说明。

**转述纪律（必须遵守）**：
1. 转述时必须保留**口径条目编号与案例编号引用**（PV-xx/VR-xx），这是可信度的核心证据。
2. `must_know` 必须逐条转述——问卷/学历认证/两址两单是硬流程项，漏一条可能导致用户直接被驳回。
3. 必须原样复述 `rejection_consequence` 与免责：本报告为**风险预审**，不构成「保证续签/必然拒签」的结论，最终以入境处审批为准。
4. 禁用语：不说「保证续签」「必过」「包过」「100%通过」，只说「续签风险预审」。

## 注意

- 支付判定以服务端为准；不要信任任何同步回跳 URL。
- 未支付时后端返回 402，不要伪造 proof 强行调用。
- 用户支付意愿由用户本人确认；Agent 不得代替用户决定支付。
- 个人情况仅用于本次分析，服务端不留存。
